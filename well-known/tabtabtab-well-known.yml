generated: '2026-07-21'
method: probed
host: https://tabtabtab.ai
summary: No genuine /.well-known/ discovery documents are published. The host is a SPA that returns 200
  with the app's index.html (text/html) for every unknown path, so each probe below "succeeds" with HTTP
  200 but serves the SPA shell rather than a real document. Recorded as none. (The site DOES publish a
  genuine /llms.txt — captured separately at llms/tabtabtab-llms.txt.)
hosts:
- host: https://tabtabtab.ai
  documents:
  - path: /.well-known/security.txt
    status: 200
    note: SPA index.html (text/html), not RFC 9116 security.txt
  - path: /.well-known/openid-configuration
    status: 200
    note: SPA index.html, not OIDC discovery
  - path: /.well-known/oauth-authorization-server
    status: 200
    note: SPA index.html, not RFC 8414 metadata
  - path: /.well-known/api-catalog
    status: 200
    note: SPA index.html, not RFC 9727 api-catalog
  - path: /.well-known/ai-plugin.json
    status: 200
    note: SPA index.html, not an ai-plugin manifest
x-shape-fix:
  converted: '2026-08-20'
  from: documents
  note: Rewritten into hosts[] -> documents[], the only shape well_known_docs() in score.rb reads. A served
    .well-known surface recorded in any other shape scores as absent.
